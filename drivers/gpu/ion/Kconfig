menuconfig ION
	tristate "Ion Memory Manager"
	help
	  Chose this option to enable the ION Memory Manager.

config ION_OMAP
	tristate "Ion for OMAP"
	depends on ARCH_OMAP3 && ION
	help
	  Choose this option if you wish to use ion on OMAP4.

